# numstore — карта проекта (project-notes)

Реферальная витрина красивых номеров на API Безлимит. Партнёр id **800848**. Некастодиально.
Истина по задаче: `TZ.md`. Живые сценарии: `docs/numstore.usecases.md`.

## Что где (архитектура v4 — SEO-каркас)
- `build.py` — **источник HTML** (~50 страниц): главная + категории ×5 + паттерны ×7 (вкл. 777/888/999) +
  лонгтейл-лендинги ×2 (LANDINGS: /dlya-biznesa/, /vip-nomera/ — витрина + cat-фильтр + tmin-порог тарифа + уникальный
  текст + FAQ; 3 бесполезные подборки удалены UC-54) + тарифы
  ×10 + `/tarify/` + коды `/kody/`+`/kod/<pfx>/`×16 + `/skolko-stoit-nomer/` + `/blog/`(16 статей, Article-schema; контент под длинный хвост, статьи в списке `BLOG` — slug/h1/title/desc/body с внутр. ссылками) + `/faq/`(FAQPage) +
  `/kak-eto-rabotaet/` (промо-лендинг: инфо-блоки + CTA в каталог, без витрины) +
  `/o-servise/` + `/politika/` + `/polzovatelskoe-soglashenie/` + 404 + sitemap(lastmod)/robots/CNAME.
  Скорость: preconnect/dns-prefetch (api Безлимит, Метрика, GTM). Organization schema: description + contactPoint.
  SEO: og:image ПОД ТИП страницы (make_og/PIL → dist/og/<kind>.png ×7; шаблон {og_image}+twitter:image; обёртка
  render_page с дефолтом /og.png), favicon.svg, Organization+WebSite schema (home_schema), Breadcrumb/FAQPage.
  Пресеты страниц: `window.PAGE` = {cat|mask|tariff}. Данные: CATEGORIES/PATTERNS/BLOG. PAGE_TMPL+VITRINA; header_block/
  main_top/scripts настраивают страницу (контентные — scripts=""). Паттерны: `window.PAGE.mask`
  (🔴 буквы-маски на API не подтверждены). Категории: `window.PAGE.cat`.
- `nav.js` — шторка (выдвижное меню, на всех страницах). `_DRAWER` в build.py (`_build_drawer`) прокинут во все страницы; группы по порядку: **Каталог** (Все номера/Подбор по красоте/Номера по кодам/Тарифы) · **Подборки** (лонгтейл-лендинги) · **Сервисы** (Проверить красоту/Оператор/Сколько стоит/Динамика цен/API) · **Информация** (Как это работает/FAQ/Блог/О сервисе) · **Telegram** · **Документы**. Категории/паттерны — в шапке/футере.
- `deploy.sh` — сборка+пуш dist в public Pages-репо (`GH_TOKEN=… ./deploy.sh`). Токен из env, не хардкод.
- `calc.js` — калькулятор: цена НОМЕРА из офиц. прайса (pmin/pmax в CATEGORIES: brilliant 400к–4М,
  platinum 25к–600к, gold 50к–200к, silver 12к–100к, bronze 3к–12к ₽) + живая абонплата тарифа/мес из API.
- `_sources/` (gitignore) — офиц. прайс «Стоимость номеров.pdf» (скан, читан через fitz→PNG→Read) + юр-доки
  Безлимит (оферты/политика/согласия/договор/реквизиты ООО Безлимит ИНН 9725007063) — для юр-страниц сайта.
  Прайс: маска в нотации A/B=повтор, %=любая (конвертится в наш phone_pattern). Дропзона (порт 8000) очищена.
- 📌 Публичные юр-доки — источник правды: **bezlimit.ru/files/** (обновлять оттуда при изменениях).
- 📌 Из КАБИНЕТА ДИЛЕРА (на публичном /files/ НЕТ): `dealer_dogovor-okazaniya-uslug.pdf` (договор оказания услуг,
  публичная оферта, 16 стр) + `dealer_rekvizity-bezlimit.pdf` (реквизиты ООО Безлимит).
- `index.html` — dev-копия витрины (может дрейфовать; деплой = `dist/`, генерённый build.py).
- `app.js` — витрина (клиентский fetch, фильтры, бронь) + читает `window.PAGE={cat}` для категорийных страниц.
- `config.js` — `API_BASE`, публичный Basic-токен Безлимит, `REF_STORE_URL`, `REF_ID` (=800848, идёт в user_id брони).
- `app.js` — клиентский fetch (mask-category, широкий пул `9NNNNNNNNN`), `flatten` в плоский список с `._cat`,
  сайдбар-фильтры, пагинация, маска-поиск, прямая бронь `reserve`, ИЗБРАННОЕ (FAV/localStorage magz_fav, ♥, режим favMode), бейджи категорий (catBadge).
- `beauty.js` — страница подбора номеров по «красоте» (`/podbor-po-krasote/`): грузит локальный catalog.json, считает оценку 0–100 по ТЕЛУ номера (7 цифр после кода)+хвост-6, а не по всем 10 (иначе код разбавляет красоту); КОПИЯ анализатора из `checker.js` — при правке весов синхронить оба, ползунок = мин. порог красоты, сортировка ↓. Карточки `.num`+чип `.num-score`. (UC-75)
- `checker.js` — «Проверить красоту номера» (`/proverit-nomer/`): анализатор `patStrength`/`analyze` (оценка 0–100 по телу+хвост-6, вердикт, признаки; синхронно с `beauty.js`) + проверка наличия в каталоге. Разбор ввода `digitsOf`: `7/8` снимаем только у 11-значного, неверную длину не глотаем — `run()` предупреждает (UC-75: правки оценки по телу + разбора ввода).
- `chart.js` — self-contained canvas-график динамики средней абонплаты по категориям (данные `/data/price_history.json`, копятся кроном; кнопки диапазонов появляются по мере накопления). Интерактив (UC-74): тултип цен при наведении (визир+подсветка точек) + ползунок по датам для телефона. Общий для `/dinamika-cen/` и мини-графиков на страницах категорий.
- `styles.css` — тёмная премиум-тема (графит+золото), `.layout/.sidebar/.filter-*/.num*`, `.chart-*` (график/тултип/ползунок).
- `docs/` — project-notes (эта карта), numstore.usecases.md (живые сценарии UC-1..74), DEFINITION_OF_DONE.
- **Превью:** `python3 -m http.server 5173 --bind 0.0.0.0` → http://localhost:5173.
- **Репо:** github.com/sementsul/numstore (private). Пуш токеном пользователя. identity `numstore-dev`.

## Запуск / превью
- Статика: `python3 -m http.server 5173 --bind 0.0.0.0` в корне → http://localhost:5173.
- ⚠️ Каталог грузится в БРАУЗЕРЕ посетителя (его IP). Серверный/CI IP у `api.store.bezlimit.ru` в бане
  (анти-бот датацентров) — но клиенту это не мешает.

## Ключевые решения / грабли
- 🪤 **GitHub гасит scheduled-кроны после 60 дней без активности**, а коммиты бота `GITHUB_TOKEN` активностью НЕ
  считаются. Поэтому крон price_history.yml пушит ежедневную точку в numstore **под PAT (DEPLOY_TOKEN)** — PAT-пуш
  сбрасывает таймер, все кроны живут. ⚠️ DEPLOY_TOKEN должен иметь доступ к sementsul/numstore (не только magzgold);
  если шаг «Сохранить историю в numstore» падает — проверить scope токена.
- 🧭 **Репо numstore ПУБЛИЧНЫЙ** (с 2026-08-20) → GitHub Actions-минуты безлимитны (кроны без оглядки на 2000/мес).
  Аудит перед публикацией чист: в коде/истории только публичный витринный Basic-токен; `.env`, `_sources/` (личные
  юр-доки), `bot/config.php` (токен бота) — в .gitignore, никогда не коммитились. Секреты — в GitHub Secrets.
  🔴 Не коммить секреты/личные данные — репо теперь виден всем.
- 🪤 **ГРАБЛИ `PAGE`:** в app.js есть КОНСТАНТА `PAGE = 60` (размер страницы) и пресет `window.PAGE`. НЕ объявлять
  локальную `var PAGE` под пресет — из-за var-hoisting это одна переменная, пресет перетрёт `60` на объект `{}`, и
  `shown = PAGE` → `slice(0, {})` → пусто (все фильтры/сортировка/«ещё» ломаются). Пресет зовём `PRESET` (UC-45).
- 🧭 **Гео-лендинги = SEO-разрез, НЕ фильтр** (UC-47): регион номера ≠ регион покупателя (реестр Россвязи: 66%
  номеров московские, Кавказа 0). Страницы `/krasivye-nomera/<city>/` показывают ПОЛНЫЙ каталог + локальный текст/
  доставку; в sitemap, но НЕ в верхнее меню (для поиска, не для юзеров др. регионов). База: goldnumbers/DEF-9xx.csv.
- 🧭 **Категории — только в шапке** (catnav → `/kategoriya/<slug>/`), в сайдбаре каталога их нет (UC-46).
- 🧭 **Региональные тарифы Анадырь/Норильск скрыты** (регекс в app.js `okTariff`, фильтр в fetchNumbers) — стрёмно выглядят (UC-46).
- 🧭 **Некастодиально:** не храним деньги и чужие ключи; кастоди+KYC на Безлимит; реф-id при покупке. Легалка минимальна.
- 🧭 **Клиентский fetch, а не CI-скрейп:** `api.store.bezlimit.ru` банит датацентровые IP («banned permanently»).
  Обход анти-бота НЕ делаем. Читаем из браузера посетителя (чистый IP) — CORS у API открыт (проверено с example.com).
- 🧭 **Каталог глобальный:** `/super-link/phones/collection` отдаёт одинаковый набор без store-id; реф цепляется на покупке.
- 🔶 ПРИНЯТЫЙ РИСК: API недокументированный + токен чужой (публичный, из их бандла). Пересмотреть если Безлимит
  сменит токен/эндпоинты/CORS. Митигация: авто-извлечение токена из бандла `l.bezlimit.ru` на сборке (TODO) + фолбэк.
- 🧭 **git identity = `numstore-dev`** (НЕ личное имя владельца — публичный репо).
- 🔴 Атрибуция реф-покупки к 800848 — не подтверждена деньгами (пока редирект в общий реф-стор). Снять из Network.

## Структура объекта номера (из API, expand=phones.tariff)
`{ phone: 9584949494, type, type_category, tariff:{ name, group_name, price, minutes, sms, internet, prolong_paid_price } }`
(+ вложенные поля, ещё не разобраны — там цена номера/маска).

## Поиск и фильтры (актуально)
- **Маска:** позиционная, 10 символов (цифра=точная, `N`=любая, буква=повтор). В API — параметр
  **`phone_pattern`** (НЕ `cubes`! cubes только в URL сайта Безлимит для реф-ссылки). Проверено.
- **Эндпоинт выдачи:** `GET /super-link/phones/mask-category?expand=tariff&is_reserved=false&per_page=100&phone_pattern=<маска|9NNNNNNNNN>`.
  Ответ — объект-группы `{"<категория>":{items:[номера]}}` → `flatten()` в плоский список (дедуп по phone).
- **Категория** — серверный фильтр `mask_categories=<код>` (коды из `/filters` → `mask_category`).
- **Тариф** — КЛИЕНТСКИЙ фильтр (серверный param не работает); список тарифов строим из фактических номеров
  (`buildTariffFilter`), мёртвые из `/filters` не показываем. **Цена** — клиентский тир-фильтр.
- Списки для сайдбара — из `/super-link/phones/filters` (живые, не захардкожены).

## Прямая бронь (РЕАЛИЗОВАНА)
Флоу раскрыт через XHR-перехват на странице Безлимит (их API — axios/XHR, не fetch):
- **Создание:** `POST /super-link/reservations?expand=super_link_uuid`, FormData
  `{phone, tariff_id, type:"store", user_id:"<REF_ID=800848>", filter:"professional"}` — **без** Super-Link-UUID.
  Сессия НЕ нужна заранее: этот POST сам её и создаёт.
- **Ответ:** объект брони (`id`, `phone_number`, `tariff_id`, `dealer_id:1165593`, `removed_at` ~1ч) +
  `super_link_uuid.uuid` (свежая сессия). `user_id` в теле = реф-привязка → `dealer_id` в ответе (комиссия).
- **Ссылка оформления:** `store/800848?type=p&cubes=<номер>&uuid=<super_link_uuid>`.
- В коде: клик «Забронировать» → `bookingInfo(p)` (модалка-памятка: 4 шага + 🔴 только граждане РФ, повторяет
  инфо-экран оператора, который иначе проскакивается) → кнопка «Забронировать и продолжить» → `doReserve(p)`
  (POST FormData → `deepUuid(ответ)` → открыть окно оформления). Стили `.book-modal` в styles.css.
- 🔶 РИСК: каждый клик по «продолжить» = реальная бронь из лимита 50 (авто-отмена ~1ч). Митигация: памятка-модалка. Владелец расширит лимит.
- Заголовок Content-Type НЕ ставим вручную — браузер сам ставит multipart boundary для FormData.

## Дизайн-направление (выбрано 2026-08-19)
Тёмный ЛЮКС (не DOS-ретро!): графит-фон `#0d0f13` + золото/шампань `#c9a558` акцент + светлая типографика.
Номер = ювелирный объект (крупно, разрядка, tabular-nums). Референсы: astralproperties.mc (структура
маркетплейс-карточки+фильтры), villalionview/measuna (премиум-минимализм). Взяли ВИД (элегантность/воздух/
металлик), НЕ взяли их тяжесть+тонкий контент (нам нужно много быстрых текст-богатых страниц под SEO).
Фоток нет (товар — цифры) → премиальность держим на типографике, не на медиа (→ легко и быстро = хорошо для SEO).


## SEO-каркас (сборщик)
- `build.py` — генерит `dist/`: главная + `/kategoriya/<slug>/` (5 категорий) + sitemap.xml/robots.txt/CNAME/.nojekyll.
  Уникальные title/description/H1/intro + BreadcrumbList schema + катнав. Категорийная страница: `window.PAGE={cat:"<код>"}` → app.js грузит эту категорию.
- **HTML source of truth = build.py** (VITRINA + PAGE_TMPL). Корневой index.html — dev-версия витрины (может дрейфовать; для деплоя используем dist/).
- **Сборка/превью:** `python3 build.py` → `cd dist && python3 -m http.server 5173`.

## Деплой (GitHub Pages)
- **Публичный deploy-репо: github.com/sementsul/magzgold** (только собранный dist/, БЕЗ исходников/доков).
  Источник — приватный `numstore`. Pages: source main/root, CNAME=magzgold.ru. 🟢 ЖИВОЙ: https://magzgold.ru, HTTPS enforced, сертификат approved.
- **Редеплой:** `GH_TOKEN=ghp_… ./deploy.sh` (build.py каждый раз сносит dist/.git → скрипт заново init+push).
- **DNS (делает владелец у регистратора):** apex magzgold.ru → A 185.199.108-111.153 (+AAAA 2606:50c0:8000-8003::153); www → CNAME sementsul.github.io. После DNS GitHub выпустит HTTPS (Let's Encrypt) → включить Enforce HTTPS.

## Статус
🟢 ПРОД https://magzgold.ru (HTTPS). v5: ~50 SEO-страниц (категории/паттерны/тарифы/коды/FAQ/блог/калькулятор/юр),
шторка, избранное, SEO-обвес (og/schema/favicon/lastmod), УТП «бесплатная доставка SIM по РФ + eSIM».
Беклог: Метрика/Вебмастер ID, проверка буквенных масок, юр-ревью, авто-токен. --- v4 (история): бренд MagzGold, тёмный люкс, маркетплейс-витрина (сайдбар-фильтры категория[сервер]/
тариф[клиент]/цена + густая сетка + «показать ещё» + маска-поиск + прямая бронь). SEO-каркас: сборщик build.py →
главная + 5 категорий + 404 + sitemap/robots/schema. Всё в git + github.com/sementsul/numstore (private).
Готово: паттерны, калькулятор (реальные цены из прайса), блог (3 статьи), «О сервисе». Всего ~16 страниц.
Дальше: ДЕПЛОЙ на GitHub Pages (magzgold.ru) — нужен public-репо или Pro + DNS. Позже: тарифные страницы,
юр-страницы (политика/оферта), авто-извлечение токена, проверка паттернов с буквами.

## Реквизиты (юр-справка)
Владелец/оператор сайта — **Семенцул Максим Геннадиевич, самозанятый (НПД), ИНН 381616884622**, email sementsul.maksim@yandex.ru. Источник правды — `ratescout/data.json` (первый проект). В футере лендинга. У numstore своего data.json нет (конфиг в config.js) — при интеграции в build.py вынести в конфиг.

## Прогрев каталога (UC-63)
- **UC-67:** единый кликабельный логотип изначальный лендинговый: серифный 22px, Gold жирным на всех страницах → ведёт на лендинг «/» (в т.ч. /start/, где раньше был не-ссылка h1).
- **UC-66:** единый 4-колоночный футер (о сервисе/каталог/документы/контакты + реквизиты) на всех страницах — стили .fcols в styles.css.
- **UC-65:** карточки `.num` не прячем reveal-анимацией (были невидимы); «Все номера» в навигации → `/start/`, не `/`.
- **UC-64:** ВСЕ страницы (app.js) грузят номера из локального `catalog.json`, а не из живого API — `fetchNumbers` → catalog.json (маска/фильтры на клиенте: `maskMatch`/`matchesClient`; категории `catSlugsForPage`), `fetchNumbersLive` — фолбэк. Убран `loadFilters` (лишний API). На загрузке 0 обращений к Безлимиту; API только при брони (живой чек наличия + резервация); при занятом номере — модалка «номер купили» (showTaken) на всех страницах, как на лендинге. catalog.json получил поле `i` (tariff.id). 🔶 локальная база = топ-250/категория → узкий маска-поиск ограничен этим срезом.
- `collect_catalog.py` — тянет до 250 номеров/категория из API (`_group_items` берёт ТОЛЬКО свою группу — эндпоинт отдаёт все 5, иначе в бриллиант лезут чужие; бриллиант реально ~59, тариф 4000-5000) → `data/catalog.json` (поля n/t/p + `c` — тарифный тир top/mid/base, классифицируется относительно категории, само-подстраивается). Запускать в CI (у контейнера IP к API в бане, у GH Actions — проходит).
- `.github/workflows/catalog.yml` — крон каждый час: собрать (пагинация до 250, API ~50/стр) → коммит (push с pull --rebase, устойчив к гонке) в numstore (под DEPLOY_TOKEN) → публикация в magzgold. ~1250 номеров, ~73 КБ.
- `premium-demo.html` — ПРЕМИУМ-ЛЕНДИНГ (✅ В ПРОДЕ как боевая главная magzgold.ru (build.py render_home читает этот файл + вставляет SEO-голову); превью на :3000). Текущий вид:
  - Шапка: лого + меню **Каталог**(`/start/`) · **Тарифы**(`/tarify/`) · **Блог**(`/blog/`) · **О сервисе**(`/o-servise/`).
  - Герой: eyebrow «Красивые номера · доставка по России» → H1 **«one of one»** → строка про заботу/комфорт → кнопка **«Номера»** (скролл к сетке).
  - Сетка: живые БРИЛЛИАНТОВЫЕ из `catalog.json` (класс `c=top`, случайные 9), фолбэк на живой API; кнопка **«Другие номера / Все бриллиантовые»** → `/kategoriya/brilliant/`.
  - Блок **«Почему нас выбирают»** (5 преимуществ). Футер: о сервисе + Каталог/Документы/Контакты + **реквизиты** (Семенцул М.Г., самозанятый НПД, ИНН 381616884622, email).
  - Бронь: окно с 4 шагами → живой чек + POST-резервация → ссылка с uuid (флоу 1:1 из app.js); занят → модалка «номер купили».
- ⚠️ Лендинг ещё НЕ закреплён в build.py — превью раздаётся копией в `dist/index.html` (порт 3000). После одобрения дизайна — интегрировать в render_home, бронь тогда через app.js (убрать дубль из демо).

## Автономность / CI (UC-61)
- `.github/workflows/watchdog.yml` — ежедневно тянет `data/price_history.json` из raw, возраст свежайшей точки >48ч (два пропущенных дня) → оповещение в 2 канала: **Telegram** в личный чат (`TELEGRAM_TOKEN`+`ALERT_CHAT_ID`) + **падение прогона** → письмо. Ловит: заснувший крон (истёк DEPLOY_TOKEN), поломку сбора цен, падение публикации. Секрет `ALERT_CHAT_ID` установлен → ТГ-канал активен (проверено live: sendMessage 200).
- `.github/dependabot.yml` — еженедельно следит за версиями GitHub Actions, открывает PR при обновлении.
- `.github/workflows/dependabot-automerge.yml` — safe авто-мёрж: minor/patch авто, major на ручной клик.
- `DEPLOY_TOKEN` — бессрочный classic-PAT (No expiration); держит крон живым (PAT-пуш сбрасывает 60-дн таймер).

## Премиум-редизайн (UC-62, ⏳ превью)
Серифные заголовки + воздух + появление при скролле (`styles.css` премиум-слой в конце; `.js`-класс + IntersectionObserver в PAGE_TMPL). Главная: сдержанный герой `.mhero` + `window.PAGE={cat:"brilliant,brilliant_super"}` (только бриллиант). НЕ задеплоено — ждёт одобрения владельца, потом `deploy.sh`.

- **UC-68:** растровые фавиконки (favicon.ico + png 32/192 + apple-touch) — Яндекс SVG не грузит; линки в PAGE_TMPL+лендинге, файлы в copy-списке build.py.
- **UC-69:** SEO главной — 9 бриллиантовых номеров рендерятся в HTML (SSR из catalog.json, `#bgrid`) + SEO-текст-блок `.seo-home` (ключи+перелинковка, 327→527 слов). H1 «one of one» оставлен. Крон каждый час пересобирает главную (build.py) → SSR-номера свежие ежечасно.
- **UC-70:** подписка на маски номеров — кнопка «🔔 Следить» (сайт) → deep-link в бота; `bot/bot.php` хранит подписки (watches.json) и уведомляет при появлении номера (check_watches, троттлинг 30 мин). ⚠️ bot.php перезалить на Бегет.
- **Моб. фикс:** боковой скролл на /start/ (и др. страницах с каталогом) давала off-canvas шторка `.drawer` (`position:fixed; right:0; translateX(100%)`) — уезжала за правый край. Лечится `html,body{overflow-x:clip}` (не hidden — не ломает `position:sticky` сайдбара) + `overflow-x:hidden` в моб. брейкпоинте как фолбэк для старого iOS<16. Проверено headless 380px: единственные переполняющие элементы были `.drawer*`.
- **Бот подписки:** сообщение при подписке = «✅ Вы подписались на маску … как появятся — обязательно уведомим» + inline-кнопки «✖ Отписаться» (unwatch) и «📋 Мои подписки».
- **Бот подписки — надёжность (UC-73):** боевой инцидент — после первой подписки бот вис (`check_watches` синхронно ждала медленный API + флудила уведомлениями про все номера сразу → упор в `set_time_limit(70)`). Фикс: тихий базовый снимок (подписка хранится `null` → первая проверка молча, уведомляем только про появившиеся ПОЗЖЕ), тайм-бюджет 15с, API-таймаут 8с, антиспам-кэп 20/заход, поллинг 50→45с. Запись `watches.json` теперь атомарна (`.tmp`+rename) и с проверкой успеха — при неписьменной папке бот шлёт честное «⚠️ не смог сохранить», а не ложное «подписались». **Статус:** сайт (кнопка+deep-link) ✅ на проде; бот ✅ по ручной команде (`/watch` → уведомления доходят), ⏳ финальный bot.php (MD5 6b9a544…, 22597 б) перезалить на Бегет + при ⚠️ дать папке права 775. Открытый вопрос: deep-link `/start watch_…` Telegram не всегда досылает уже-запущенному боту — разобрать после подтверждения записи.

- **Бот — корневой баг подписок (решён):** `mask_human` рвала многобайтовый «•» побайтовым `substr` → битый UTF-8 → Telegram молча отклонял сообщение (пустой `/mywatches`, немой deep-link с масками-N). Фикс: substr по ASCII, `N`→`•` в конце. +команда `/debug`. Финал bot.php MD5 ea273d3…, 23820 б — перезалить на Бегет.

- **✅ Подписки на маски — ГОТОВО (2026-08-22):** end-to-end подтверждено владельцем (сайт «🔔 Следить» → бот → /mywatches → уведомление о появлении → «Отписаться»). Корневой баг (битый UTF-8 в mask_human) устранён.

- **UC-74: GEO (Generative Engine Optimization) — 2026-08-24.** `render_llms()` в build.py генерит `dist/llms.txt` (llmstxt.org: описание + карта разделов/категорий/паттернов/блога + фиды) — раньше файла не было. `render_sitemap()` теперь пишет в robots.txt ЯВНЫЕ Allow-блоки для AI-краулеров (GPTBot/OAI-SearchBot/ChatGPT-User/ClaudeBot/Claude-Web/anthropic-ai/PerplexityBot/Perplexity-User/Google-Extended/Applebot-Extended/CCBot/Amazonbot/Bytespider/YandexAdditional) + ссылку на llms.txt. Тексты не менял — llms.txt собран из существующих desc-полей. Проверено: `python3 build.py` EXIT 0, dist/llms.txt (53 стр.) и dist/robots.txt валидны.

- **UC-75: GEO — обогащённый Product + строка-факт тарифа (категории).** 🔴 Модель: номер БЕСПЛАТНЫЙ, платят только за тариф (₽/мес); pmin/pmax ≠ цена покупки (фильтр каталога). 🔴 Тарифы меняются → мин.тариф берётся ЖИВЫМ из `data/catalog.json` (`cat_min_tariff`, крон ежечасно), поле `tmin` — только фолбэк. `cat_schema` +brand/image/url/seller, Offer `price:0` + абонплата в UnitPriceSpecification (было ошибочно lowPrice=pmin); `render_category` +видимая `<p class="cat-facts">` «номер бесплатно, тариф от <живой мин.тариф> ₽/мес» (в тон теме, интро не трогали). Счётчики НЕ брали (catalog.json обрезан капом). Главная-лендинг не тронута.

- **UC-76: SEO/AEO/GEO/LLMO/NEO добор (дизайн не трогали).** robots-meta в PAGE_TMPL+главной; Organization `sameAs` (TG-канал); `speakable` (WebPage категорий .cat-facts + блог Article); блог Article +author/dateModified/publisher.logo (datePublished не фабрикуем); `Dataset`→api/numbers.json. SearchAction пропущен.

- **UC-77: SEO — хаб городов + title.** `/krasivye-nomera/` был сиротой → добавлен в шторку (Каталог→«Номера по городам»), теперь вх.ссылки с 84 стр. Подрезаны длинные title главной/старта (~56/~52 симв, meta-only).
